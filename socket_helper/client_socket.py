import socket


class ClientSocket:
    pass


if __name__ == "__main__":
    # with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
    #     s.connect((HOST, PORT))

    #     while True:
    #         s.sendall(input().encode())

    #         data = s.recv(1024)

    #         print("Received", data) 
    pass